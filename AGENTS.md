# もぐもぐトレード 開発エージェントガイド

## プロジェクト概要

「もぐもぐトレード」は、小学生の給食における好き嫌い解消とフードロス削減を目的とした、エンターテインメント性のあるマッチングアプリです。

主な体験は次の3つです。

- 苦手な食材や提供条件を使った、クラス内のオープントレード
- 完食した生徒にポイントを付与する完食マイレージ
- 余った人気メニューをポイントで競るブラインドオークション

対象ユーザーは小学生です。UI文言、操作手順、エラー表示は短く、迷いにくく、誤操作を戻しやすい形を優先してください。

## 現在の技術スタックと実態

- 対象デバイス: iPhone / iPad
- フロントエンド: Swift / SwiftUI
- 依存性注入: Swift Dependencies
- ローカルデータ管理: UserDefaults（クラス参加情報・アレルギー名簿など）。SwiftData は未導入
- バックエンド: Firebase Firestore（導入済み）
- Xcodeプロジェクト: `mogumogu-trade.xcodeproj`（Xcode 16 / 同期グループ方式。ファイルを置けば自動でビルド対象になる。pbxproj への手動登録は不要）
- アプリターゲット: `mogumogu-trade`

実装済みの主な機能:

- クラス参加（クラスコード/出席番号/ニックネーム、UserDefaults保存、自動参加、Firestore でクラス存在確認）
- トレード（選択式の出品・リアルタイム監視・条件一致での自動マッチング。Firestore + トランザクション）
- アレルギー登録（教員入力、`AllergyChecker` の純粋判定ロジック）
- 完食マイレージ（QR承認＋ポイント台帳 `pointLedger`。下記「完食マイレージ」「QR承認」「ポイントルール」節を参照）
- ブラインドオークション（先生画面からの開催・締切、生徒の入札・待機・結果表示、Firestore 連携、参加費・落札消費のポイント台帳反映）

未実装・部分実装:

- 先生モード（画面の骨組みとオークション開催・締切は実装済み。教員PIN認証、トレード停止、強制リセット、履歴は未実装）
- アレルギー判定の実トレード統合（`AllergyChecker` はあるが、マッチング処理にはまだ組み込んでいない）
- Firestore Security Rules、SwiftData

実装時は、既存コードにない仕様を推測で広げず、この文書のMVP方針とv1決定事項を基準にしてください。本書とコードの実態が食い違う場合はコードを優先し、本書も合わせて更新してください。

## MVP方針

v1/MVPは「生徒向け中心」に固定します。

v1で優先する範囲:

- クラスコード、出席番号、ニックネームによるクラス参加
- 保存済みクラス情報による自動参加
- 今日の給食に対するトレード出品とマッチング
- 完食ポイントの獲得と継続保持
- 余り物デザート向けの簡易ブラインドオークション

v1では最小限に留める範囲:

- 本格的な教員・管理者向け専用画面
- 詳細な権限管理
- AIによる自動マッチング
- 複雑な不正検知や監査ログ

教員機能は、同一アプリ内の先生モードとして実装し、トレードやオークションを止めるための最低限の管理操作に絞ります。本格的な教員アカウント、教員ID登録、ロール設計は将来検討です。

## 主要機能仕様

### クラス参加

- 初回起動時または年度初めに、教員が提示した4〜6桁のクラスコード、自分の出席番号、画面表示用ニックネームを入力する。
- 入力情報は端末ローカルに保存し、2回目以降は保存情報を使って自動的に今日のトレード画面へ進める。
- ニックネームを扱う場合でも、v1では出席番号を主識別子として扱う。ニックネームは表示補助であり、識別・権限判定・ポイント履歴の主キーに使わない。

将来の概念候補:

- `StudentProfile`
- `ClassMembership`

### トレード

- 匿名モードは採用しない。出品者の出席番号とニックネームを常に表示する。
- 交換条件は自由記述にしない。選択式UIで指定する。
- 条件種別は、今日の給食の食材、労働力の提供、アイテムの提供を基本とする。
- v1では出品作成時の自動マッチングは行わない。生徒が「みんなの出品」から相手の出品を選び、交換操作をした時点でトレード成立とする。
- 相手の出品を選んで成立する場合は、相手の「出すもの」と「ほしいもの」を反転した自分側の成立済み出品を作る。
- 成立した当事者の画面には完了通知を出す。
- アレルギーに該当する食材が関係するトレードは、v1では生徒画面のUIで出品または交換操作をブロックする。

将来の概念候補:

- `TradeOffer`
- `TradeCondition`
- `TradeMatch`
- `FoodItem`

### 完食マイレージ（実装済み）

- 完食した生徒は、隣の席などのクラスメイトに空のトレイを確認してもらう。
- **完食した本人の端末に承認用QRコードを表示し、確認者の端末で読み取る。**（当初案は逆向きだったが、書き込み主体を確認者にして自己承認をしにくくするため向きを反転した）
- 確認者側の検証（クラス一致・自己承認禁止・5分有効期限）を通過したら、確認者の端末から完食ポイントを付与する。
- ポイントは日を跨いで継続的に蓄積し、給食時間終了でリセットしない。
- ポイントは残高を直接持たず、Firestore の `classes/{classId}/pointLedger` に履歴として追記し、合計で残高を出す。

実装の対応箇所:

- 台帳モデル: `Models/PointLedger.swift`（`PointLedgerEntry` / `PointEntryType`）
- 付与・残高購読: `Dependencies/MileageClient.swift`（`awardMealCompletion` / `observeBalance`）
- QRトークン: `Models/QRPayload.swift`、生成・パース: `Dependencies/QRCodeClient.swift`
- 画面・検証: `Features/QRCheck/`（検証は純粋関数 `QRCheckViewModel.validate`）
- Firestore 設計と想定 Security Rules: `docs/firestore-pointLedger.md`

### ブラインドオークション

- プリンやゼリーなど、クラス内で人気メニューの余りが出た場合にのみ開始する。
- v1では、生徒側から見た参加・入札・結果確認を優先する。
- 生徒は保有ポイントの範囲内で任意の額をベットする。
- 締め切りまでは他者のベット額や参加状況を表示しない。
- 最高ベット額の生徒が落札する。
- 同額最高入札が複数ある場合は、先に入札した生徒を勝者にする。
- v1の在庫は一旦1個固定とし、落札者は最大1人にする。
- 同じクラス・同じ給食日で同時に開催できるオークションは1件だけにする。
- 初回入札時に参加費として全員5ポイントを消費する。再入札では参加費を追加消費しない。
- 落札者は参加費に加えて、締切時にベット分のポイントを追加で消費する。
- 締切時に最高入札者の残高が不足している場合は、支払い可能な次点へ繰り下げる。
- 中止機能と参加費返金はv1では未実装とする。

将来の概念候補:

- `AuctionRoom`
- `AuctionBid`
- `AuctionResult`

## データ管理方針

ローカル保存とバックエンド同期は、役割を分けて設計してください。

- UserDefaults: クラスコード、出席番号、ニックネーム、初回起動済みなど小さな設定値
- SwiftData: 端末内で参照するプロフィール、ポイント履歴、下書き状態、同期待ち状態
- Firestore: クラス単位で共有されるトレード、オークション、ポイント台帳

ポイントは単なる合計値だけでなく、履歴ベースで扱う方針を優先してください。落札消費、完食付与、取り消しなどを後から確認できるためです。

Firestoreスキーマはまだ確定していません。実装時にスキーマを追加する場合は、クラス単位のデータ分離、読み取り範囲、書き込み権限、同時更新を先に設計してください。

## 安全・セキュリティ方針

- アレルギー情報に該当する食材が渡るトレードは、v1ではUI上で選択不可または交換不可にする。
- 小学生向けのため、自由記述を増やさない。トラブルや不適切入力を避けるため、選択式を優先する。
- 匿名ではなくオープンな市場にする。ただし、必要以上の個人情報は表示しない。
- 教員または管理者が、進行中のトレードやオークションを停止・リセットできる余地を残す。
- Firebase導入時は、クライアント側だけで安全性を担保しない。Firestore Security Rulesを前提にする。

## SwiftUI実装ルール

- 小学生が使う画面として、主要操作を大きく、短い文言で配置する。
- 画面の最初に使い方説明を長く置かず、操作そのものが分かるUIにする。
- 出品、承認、入札、落札などの重要操作は、完了状態と失敗状態を明確に表示する。
- 食材、条件、ポイントなどはカードやリストで視認性を高くする。
- 画面遷移や状態管理は、後からFirebase同期を追加しやすいようにView内へ直接詰め込みすぎない。
- プレビューを追加できる画面では、正常系、空状態、エラー状態のプレビューを用意する。

## Swift Dependencies方針

外部サービス、永続化、時刻、UUID、QR生成・読み取り、Firestoreアクセスなどの副作用は、Swift Dependenciesを使って注入してください。

- ViewからFirebase、SwiftData、UserDefaults、カメラ、QR処理を直接呼ばない
- 実装依存の処理はClient型やService型に切り出し、Dependencyとして差し替え可能にする
- Previewやテストでは、実データ通信を行わないmock/stub dependencyを使う
- 日付、現在時刻、UUID生成、乱数、タイマーなども直接生成せず、テスト可能なdependencyとして扱う
- 依存関係を増やす場合は、画面都合ではなく機能境界に合わせて追加する

将来の概念候補:

- `ClassClient`
- `TradeClient`
- `MileageClient`
- `AuctionClient`
- `QRCodeClient`

## Firebase / SwiftData導入時の注意

- Firebaseを追加するまでは、リポジトリに設定ファイルや依存関係がない前提で作業する。
- `GoogleService-Info.plist` などの環境依存ファイルを追加する場合は、コミット対象としてよいか先に確認する。
- SwiftDataモデルを追加する場合は、Firestore同期用モデルと画面表示用モデルの責務を混ぜすぎない。
- Firestoreの同時更新が関わる箇所、特にポイント消費とオークション落札は、トランザクションまたはそれに相当する整合性確保を前提にする。
- QR承認は本人確認の強度が低いため、v1では簡易承認として扱い、厳密な不正防止は将来検討にする。

## ビルド・確認手順

ビルドやスキーム確認は、利用できる場合は xcodemcp を優先してください。

- プロジェクト: `/Users/hosonuma/Desktop/swift/mogumogu-trade/mogumogu-trade.xcodeproj`
- スキーム: `mogumogu-trade`

xcodemcpでの確認:

- スキーム確認: `xcode_get_schemes`
- 基本のビルド確認: `xcode_build`

`xcode_build` には、上記のプロジェクト絶対パスと `mogumogu-trade` スキームを渡してください。destinationを指定しない場合は、Xcodeの現在のアクティブdestinationが使われます。

xcodemcpが使えない環境でのフォールバック:

```sh
xcodebuild -list -project mogumogu-trade.xcodeproj
xcodebuild -project mogumogu-trade.xcodeproj -scheme mogumogu-trade -destination 'generic/platform=iOS' build
```

ドキュメントのみを変更した場合、アプリのビルド実行は必須ではありません。コード、依存関係、プロジェクト設定を変更した場合は、できるだけ xcodemcp の `xcode_build` で確認してください。

## v1決定事項

### AIマッチング

AIマッチングはv1に含めません。v1では、食材、労働力、アイテムなどの選択式条件で出品を作り、生徒が「みんなの出品」から相手を選んだ場合だけトレード成立とします。出品作成時に条件一致を探して自動成立させる処理はv1に含めません。

将来機能として、苦手食材や過去の成立履歴を使ったおすすめ表示を検討できます。ただし、小学生向けの食事・アレルギーに関わるため、AIの提案だけでトレードを成立させてはいけません。

### 教員画面

v1では、生徒アプリと同じアプリ内に「先生モード」を用意します。別アプリにはしません。

先生モードで扱う操作は最小限にします。

- オークションルームの作成
- 余り物メニューの選択
- 進行中トレードの停止
- 進行中オークションの停止
- クラス全体の強制リセット

生徒向け画面と先生モードは、見た目と導線を明確に分けてください。生徒が誤って管理操作を押せるUIにしてはいけません。

### 教員認証

v1では、教員専用アカウント登録は作りません。先生モードへ入るには、クラスコードと教員PINを使います。

現状の実装では、先生画面への導線とクラスコード入力はあるが、教員PIN認証はまだ未実装です。Firestore Security Rules も未デプロイのため、先生画面の Firestore 書き込みは現時点ではクライアント側実装に依存しています。

- 生徒: クラスコード + 出席番号 + ニックネームで参加
- 教員: クラスコード + 教員PINで先生モードへ入る

教員PINは、教員がクラスルームを作成するときに設定する想定です。Firestore導入時は、PINを平文で保存しない方針にしてください。

将来的に学校単位で運用する場合は、Firebase Authや管理者ロールを導入して、教員アカウント方式へ移行します。

### 余り物デザートの選択権限

v1では、余り物デザートを選択してオークションを開始できるのは教員のみとします。給食当番には許可しません。

給食当番による作成は便利ですが、誤登録やトラブルが起きやすいため、v1では対象外にします。

### アレルギー情報

アレルギー情報の登録・更新は教員のみが行います。生徒本人による登録・編集はできません。

基本フロー:

- 教員がクラス内の生徒ごとにアレルギー食材を登録する
- 生徒画面では、自分のアレルギー対象食材を含む出品作成や交換操作を選択不可にする
- 相手のアレルギー対象食材を渡す交換も、生徒画面のUIで交換不可にする

v1ではアレルギーブロックはUIで行います。Firestore Security Rulesやサーバ側の厳密な成立拒否は、バックエンド運用を固める段階で追加検討します。

### 生徒のニックネーム

v1では、トレード、完食承認、オークション結果の表示に出席番号とニックネームを使います。

ニックネームは本名入力を求めず、短い表示名として扱います。出席番号は必ず併記し、ニックネームだけで生徒を識別しないでください。

ニックネームのv1ルール:

- 1〜8文字
- 改行不可
- 絵文字、制御文字は不可
- 空白だけの入力は不可
- 後から変更できるが、変更してもポイント履歴や過去の出品者識別は出席番号で追えるようにする

表示例:

- `12番 もぐ`
- `出席番号 12 / もぐ`

### QR承認（実装済み）

完食マイレージのQR承認は、簡易承認として実装済みです。

- **完食者がQRを表示し、確認者が読み取って加点する**（当初案から向きを反転。書き込み主体を確認者にする）
- QRコードの有効期限は5分（`QRCheckViewModel.expirySeconds`）
- 同じ生徒が同じ日に獲得できる完食ポイントは1回まで
- 自分のQRを自分で読み取る自己承認は禁止（出席番号で判定）
- 承認者（確認者）と完食者の出席番号をポイント台帳に残す
- 「1日1回」と「通信失敗時の重複付与防止」は、台帳の決定論ドキュメントID `meal-{mealDate}-{eater}` ＋トランザクションで両立する

厳密な不正検知や座席表との連動はv1では対象外です。Firebase Auth 導入後は `docs/firestore-pointLedger.md` の Security Rules を有効化する想定です。

### ポイントルール

v1のポイントルールはシンプルに固定します。

- 完食1回: 1ポイント付与（当初は10ポイント案だったが、刻みを細かくして1ポイントに変更）
- 1日の完食ポイント付与: 1回まで
- ポイント残高の上限: なし（当初の999上限は撤廃）
- オークション初回入札: 参加費5ポイントと入札額を支払える場合のみ可能
- オークション再入札: 参加費は追加消費せず、現在の保有ポイント以下の入札額のみ可能
- オークション落札者: 参加費5ポイントに加えて入札ポイントを消費
- オークション非落札者: 参加費5ポイントのみ消費
- 取り消し: 教員だけがポイント台帳に取り消し記録（`teacherCancel`）を追加できる（未実装）

ポイントは残高の直接上書きではなく、台帳（`pointLedger`）の合計として扱う。`Student.point` フィールドは残高には使わない。オークションのアトミックな消費のため、`classes/{classId}/pointBalances/{studentNumber}` をキャッシュ残高として導入し、台帳追記と同一トランザクションで更新する。

### Firestore構造とSecurity Rules

v1では、クラス単位でデータを分離する構造にします。

想定コレクション:

- `classes/{classId}`
- `classes/{classId}/students/{studentId}`（実装済み）
- `classes/{classId}/trades/{tradeId}`（実装済み）
- `classes/{classId}/pointLedger/{ledgerId}`（実装済み。完食は決定論ID `meal-{mealDate}-{eater}`。詳細は `docs/firestore-pointLedger.md`）
- `classes/{classId}/pointBalances/{studentNumber}`（実装済み。オークション消費用のキャッシュ残高）
- `classes/{classId}/auctions/{auctionId}`（実装済み。詳細は `docs/firestore-auctions.md`）
- `classes/{classId}/auctions/{auctionId}/bids/{bidId}`（実装済み）
- `classes/{classId}/auctionActiveRooms/{mealDate}`（実装済み。同じ給食日の同時開催防止用）

基本ルール（Security Rules はまだ未デプロイで、現状はクライアント側で検証）:

- 生徒は、自分が参加しているクラスのデータだけ読める
- 生徒は、自分の出品、入札、QR承認に必要なデータだけ書ける
- 生徒は、ポイント残高を直接書き換えられない
- 教員は、担当クラスの管理データを書き換えられる
- アレルギー、ポイント取り消し、強制リセットは教員だけが実行できる

ポイント消費とオークション落札は同時更新が起きるため、FirestoreのトランザクションまたはCloud Functionsで整合性を守る前提にしてください。
